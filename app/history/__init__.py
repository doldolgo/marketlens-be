"""가격 변동 이력(히스토리) 서브시스템.

김프/역프 통계의 재료가 되는 **가격 변동 로그**를 수집·압축 저장·조회한다.

구성
    codec.py    — 무손실 압축 코덱 (델타 + varint + zstd). 이 서브시스템의 심장.
    upbit.py    — 업비트 초봉 수집기 (거래소별 코드는 공유하지 않는다)
    binance.py  — 바이낸스 1초봉 수집기
    hana.py     — 하나은행 고시환율(USD/KRW) 수집기
    store.py    — 이력 테이블 읽기/쓰기의 단일 창구
    service.py  — 변동 로그 조립, 주/월 구간 계산, 일 단위 팩킹

저장 원칙
    - DB 에는 **절대 타임라인**(epoch 초)으로 저장한다.
      "몇 초 뒤에 변했는가"(상대 시간)는 API 응답을 만들 때 계산한다.
    - 가격이 **변한 순간만** 저장한다. 같은 가격이 이어지는 구간은
      가격 경로에 정보가 없으므로 버려도 손실이 아니다.
    - 완결된 하루(UTC)는 압축 청크(`price_chunks`/`fx_chunks`) 한 행으로,
      아직 진행 중인 오늘은 스테이징(`price_points`/`fx_points`) 행으로 둔다.
"""
