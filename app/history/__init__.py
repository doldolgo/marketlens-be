"""김프/역프 기록(아카이브) 서브시스템.

기록/통계 창의 데이터(``premium_archive``)를 만든다.

구성
    upbit.py    — 업비트 초봉·분봉 수집기 (거래소별 코드는 공유하지 않는다)
    binance.py  — 바이낸스 1초봉 수집기
    service.py  — 김프/역프 계산식, 주/월 구간 계산, 대량 채우기 로직

데이터 흐름
    실시간: POST /refresh 가 market_snapshots 를 갱신한 직후, 스냅샷의
    체결측 호가와 그 국내 거래소의 KRW-USDT 호가(김프=ask · 역프=bid)로
    김프/역프를 계산해 아카이브에 한 줄 추가한다.

    과거: scripts/bulk_archive.py 가 아카이브의 첫/마지막 시각 밖 구간을
    거래소 캔들(코인=초봉 · 환율=업비트 KRW-USDT 분봉)로 한 번에 채운다.
    캔들에는 호가가 없어 **과거 ask1/bid1 은 복원 불가**다 — 백필 구간은
    종가로 근사하며 fwd/rev 가 대칭으로 나온다.

저장 원칙
    - 절대 타임라인(epoch 초)으로 저장하고, 조회 API 가 상대 시간
      ("직전 기록에서 몇 초 뒤")으로 변환한다.
    - 압축 없이 일반 행으로 저장한다 — 용량이 문제가 되면 그때 다시 검토.
"""
