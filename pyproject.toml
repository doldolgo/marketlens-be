[tool.pytest.ini_options]
# __init__.py 없이도 app 패키지를 import 할 수 있도록 프로젝트 루트를 sys.path 에 넣는다.
pythonpath = ["."]
testpaths = ["tests"]
# async 테스트/픽스처를 데코레이터 없이 그대로 돌린다 (pytest-asyncio).
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
